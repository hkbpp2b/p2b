# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run build
        env:
         VITE_HERO_TSV_URL: ${{ secrets.VITE_HERO_TSV_URL }}
          VITE_TSV_PENDETA_URL: ${{ secrets.VITE_TSV_PENDETA_URL }}
          VITE_TSV_RENUNGAN_URL: ${{ secrets.VITE_TSV_RENUNGAN_URL }}
          VITE_GIVING_TSV_URL: ${{ secrets.VITE_GIVING_TSV_URL }}
          VITE_IBADAH_CSV_URL: ${{ secrets.VITE_IBADAH_CSV_URL }}
          VITE_FUNGSIONARIS_TSV_URL: ${{ secrets.VITE_FUNGSIONARIS_TSV_URL }}
          VITE_OTHER_URL: ${{ secrets.VITE_OTHER_URL }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - uses: actions/deploy-pages@v4